<% if resource.errors.any? %>
  <div class="notification content is-danger form-errors">
    <button class="delete"></button>
    <ul>
      <% resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
